# Google Python Class Exercises - Nsikan Ekpo

This repository contains my solutions and exercises completed from the **Google Python Class** course.  
The exercises cover core Python concepts, file handling, text processing, and basic scripting.

## Completed Exercises

### 1. Baby Names
- Parsed text files containing baby names and ranks.
- Implemented functions to extract names and sort them.
- Tested command-line script usage and output formatting.

### 2. Wordcount
- Implemented a script to count word occurrences in text files.
- Added options to list all word counts or top counts.
- Practiced text processing and dictionaries in Python.

### 3. Copy Special (`copyspecial.py`)
- Scans directories for “special” files with double underscores (`__word__`) in filenames.
- Can list, copy, or zip special files via command-line options.
- Reinforced file I/O, regular expressions, and command-line argument parsing.

### 4. Log Puzzle (`logpuzzle.py`)
- Parses log files to extract puzzle image URLs.
- Downloads images to a specified directory.
- Strengthened skills with file handling, URL processing, and Python scripting.

### 5. Sorting and List Exercises
- Completed `list1.py` and `list2.py` exercises from the sorting section.
- Practiced Python lists, loops, and sorting functions.

## Key Skills Practiced
- Python 3 syntax and standard library
- Command-line scripting
- File and directory operations
- Regular expressions
- URL handling and downloads
- Data structures: lists, dictionaries, tuples
- Writing reusable, modular Python code

## How to Run Scripts
1. Ensure Python 3 is installed.
2. Navigate to the exercise folder.
3. Activate virtual environment (optional but recommended):
    ```bash
    source venv/bin/activate  # Linux/Mac
    .\venv\Scripts\activate   # Windows
    ```
4. Run scripts with:
    ```bash
    ./script_name.py [options] <arguments>
    ```
5. For `copyspecial.py` and `logpuzzle.py`, see individual usage comments at the top of each file.

## Notes
- Downloaded images or generated files are **not included** in this repository.
- Virtual environment, cache, and IDE files are excluded via `.gitignore`.
- This work is part of self-learning in Python programming fundamentals.

